#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiplying  two integers
 * @a: first integer
 * @b: second integer
 * Return: the multiplication of a number
 */

int mul(int a, int b)
{       
        return (a * b);
}       
