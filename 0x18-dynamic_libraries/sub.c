#include <stdio.h>
#include <stdlib.h>

/**
 * sub - sub two integers
 * @a: first integer
 * @b: second integer
 * Return: subtracted result
 */

int sub(int a, int b)
{       
        return (a - b);
}
