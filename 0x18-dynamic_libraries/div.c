#include <stdio.h>

/**
 * division - the division of  two integers
 * @a: first integer
 * @b: second integer
 * Return: the division two
 */

int div(int a, int b)
{       
        return (a / b);
}       
