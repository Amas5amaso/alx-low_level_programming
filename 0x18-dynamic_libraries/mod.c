#include <stdio.h>
#include <stdlib.h>

/**
 * mod - get the modules of two integers
 * @a: first integer
 * @b: second integer
 * Return: the modules of an integer
 */

int mod(int a, int b)
{       
        return (a % b);
}       
